# App
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Supabase (public — safe for browser)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Supabase (server-only — never expose to client)
# Required for: newsletter, contact form, analytics, search logs, Stripe webhook
SUPABASE_SERVICE_ROLE_KEY=

# Supabase Auth → Google (paste into Dashboard → Authentication → Providers → Google)
SUPABASE_AUTH_GOOGLE_CLIENT_ID=
SUPABASE_AUTH_GOOGLE_CLIENT_SECRET=

# Supabase Auth → GitHub (paste into Dashboard → Authentication → Providers → GitHub)
SUPABASE_AUTH_GITHUB_CLIENT_ID=
SUPABASE_AUTH_GITHUB_CLIENT_SECRET=

# Google Gemini AI (server-only) — primary free tier for all users
GEMINI_API_KEY=

# Groq free tier (optional fallback) — console.groq.com — Llama 3.3 70B, 30 req/min
GROQ_API_KEY=
GROQ_MODEL=llama-3.3-70b-versatile

# OpenRouter free models (optional fallback) — openrouter.ai — no credit card
OPENROUTER_API_KEY=
OPENROUTER_FREE_MODEL=meta-llama/llama-3.3-70b-instruct:free

# Stripe (server-only) — powers Upgrade to Pro
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_ID_PRO_MONTHLY=

# ─── Share link cron cleanup ─────────────────────────────────────────────────
# Random string (e.g. openssl rand -hex 32). Cloudflare Cron / worker calls:
#   GET https://tools.farvixo.com/api/cron/cleanup-shares
#   Header: Authorization: Bearer <CRON_SECRET>
# Hosting = Cloudflare Pages (not Vercel). See docs/NEXT_STEPS_BN.md §8.
CRON_SECRET=

# Salt for hashing anonymous client IPs in the durable rate limiter
# (public.incr_rate_counter). Required — anonymous AI image generation returns a
# 500 config error if unset. Random string, e.g. openssl rand -hex 32.
RATE_LIMIT_SECRET=

# WeatherAPI.com (server-only, optional) — enriches World Weather Pro with severe alerts + moonrise/moonset. Free key at weatherapi.com. Without it the tool still works via Open-Meteo.
WEATHERAPI_KEY=

# Firebase Web (farvixo-production-2026) — Analytics / App Check / FCM / RC
# Auth stays on Supabase. Use NEXT_PUBLIC_ for browser (Next.js requirement).
NEXT_PUBLIC_FIREBASE_API_KEY=AIzaSyCW6-ik0yBIkrHH2ScoZFEFD3u5qhUS5SU
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=farvixo-production-2026.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=farvixo-production-2026
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=farvixo-production-2026.firebasestorage.app
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=282376161241
NEXT_PUBLIC_FIREBASE_APP_ID=1:282376161241:web:d0bca9dcc89c299e1f7bc8
NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=G-VTGV5YE47C
NEXT_PUBLIC_FIREBASE_RECAPTCHA_SITE_KEY=6Le0QlctAAAAACzzpyTcXrbNbyNtZd9ri41gcKd7
NEXT_PUBLIC_FIREBASE_VAPID_KEY=mG2g3kuPlJrJULoTWIkp3NANQap_I3Ef7E_8pAAIrqo
NEXT_PUBLIC_FIREBASE_APPCHECK_DEBUG_TOKEN=your_appcheck_debug_token_here
