Build the Home page first as the master design system. Match the approved design pixel-perfect. After the Home page is finalized, extract all reusable widgets (Header, Hero, Cards, Category Cards, Tool Cards, Buttons, Navigation, Theme Components) into a global design system. Then apply the same design system to every page. Finally, fix the global Theme Engine so Light, Dark, and Custom themes update the entire app consistently with zero hardcoded colors. Preserve responsive behavior, smooth animations, state management, edge-to-edge layout, and production-quality Flutter architecture. Do not move to any other page until the Home page is 100% complete and matches the reference exactly.